{
  "name": "timothy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev:api": "pnpm --filter @timothy/api run dev",
    "dev:cli": "pnpm --filter timothy-cli run dev",
    "test": "pnpm -r run test",
    "lint": "oxlint --config oxlint.json packages"
  },
  "devDependencies": {
    "oxlint": "^1.65.0",
    "typescript": "^6.0.3"
  }
}
