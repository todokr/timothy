# Web UI（ファイル一覧 & アップロード）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Timothy の API に、ブラウザからファイルの一覧・アップロード・削除ができる管理画面を追加する。

**Architecture:** `hono/jsx` によるサーバーサイドレンダリングで一覧ページを描画し、アップロードと削除は既存の JSON API（`POST /upload` / `DELETE /files/:id`）をインライン JavaScript の `fetch` で呼ぶ。アップロードは CLI と同じく署名付き URL を取得してブラウザから GCS に直接 PUT する。ビルドパイプラインと npm 依存の追加は発生しない。

**Tech Stack:** Hono 4 / hono/jsx / hono/css / TypeScript 6 (NodeNext) / vitest 4 / firebase-admin / @google-cloud/storage

## Global Constraints

- 設計書: `docs/superpowers/specs/2026-08-07-web-ui-design.md`。矛盾があれば設計書が優先。
- 作業ブランチは `feature/web-ui`。すでにチェックアウト済み。
- TypeScript は `strict: true`、モジュールは `NodeNext`。**相対 import は必ず `.js` 拡張子を付ける**（`.tsx` ファイルを import する場合も `.js`）。
- テストファイル名は必ず `*.test.ts`。vitest の `include` が `src/**/*.test.ts` のため、`.test.tsx` は実行されない。テストコード内で JSX は書かない。
- `packages/api/vitest.config.ts` のカバレッジ閾値は `src/routes/**` に対して lines 80 / functions 50 / branches 80 / statements 80。新規の `src/routes/web.tsx` もこの対象に入る。
- UI の文言はすべて日本語。
- `HTML_FILES_COLLECTION` の値は `"htmlFiles"`。
- 署名付きアップロードの Content-Type は `"text/html; charset=utf-8"`（`UPLOAD_CONTENT_TYPE`）。
- コマンドはリポジトリルートから実行する。API のテストは `pnpm --filter @timothy/api run test`。

---

### Task 1: JSX ビルド設定の確定と作業中ファイルの整理

作業ツリーには JSX を有効化する未コミットの設定変更と、書きかけの `web-upload.tsx` が残っている。以降のタスクの土台としてまず設定だけを確定させ、書きかけのファイルは削除する（Task 4 で `web.tsx` として作り直す）。

**Files:**
- Modify: `packages/api/tsconfig.json`（未コミットの変更を確定）
- Modify: `package.json`（未コミットの変更を確定）
- Modify: `pnpm-lock.yaml`（未コミットの変更を確定）
- Delete: `packages/api/src/routes/web-upload.tsx`

**Interfaces:**
- Consumes: なし
- Produces: `packages/api/tsconfig.json` に `jsx: "react-jsx"` と `jsxImportSource: "hono/jsx"` が入った状態。以降のタスクで `.tsx` が書ける。

- [ ] **Step 1: 現在の未コミット変更を確認する**

Run: `git status --short && git diff packages/api/tsconfig.json package.json`

期待する差分は次の 2 つ。これ以外の差分があれば、そのファイルはコミットに含めず、内容を報告してから先に進むこと。

`packages/api/tsconfig.json` の `compilerOptions` に 2 行追加:

```json
    "jsx": "react-jsx",
    "jsxImportSource": "hono/jsx"
```

ルート `package.json` の `devDependencies` に 1 行追加:

```json
    "typescript": "^6.0.3"
```

- [ ] **Step 2: 書きかけのファイルを削除する**

Run: `rm packages/api/src/routes/web-upload.tsx`

このファイルは JSX として不正な内容（`<html>` の中に HTML コメント形式の TODO が直接書かれている）なので、修正ではなく削除する。Task 4 で `web.tsx` を新規に作成する。

- [ ] **Step 3: ビルドとテストが通ることを確認する**

Run: `pnpm --filter @timothy/api run build && pnpm --filter @timothy/api run test`

Expected: どちらも成功。`web-upload.tsx` を消したので型エラーは出ないはず。

- [ ] **Step 4: コミット**

```bash
git add packages/api/tsconfig.json package.json pnpm-lock.yaml
git commit -m "chore(api): enable hono/jsx in tsconfig"
```

---

### Task 2: ファイル一覧の組み立てを `lib/files.ts` に切り出す

現在 `routes/list.ts` に書かれている「Firestore から取得して一覧 DTO に整形する」処理を `lib/files.ts` に移す。Task 4 の Web UI からも同じ処理を使うため。

`listFiles` は Hono の `Context` ではなくベース URL の文字列を受け取る。ヘッダー解決の責務を `resolveBaseUrl` に分離することで、両者を独立にテストできる。

**Files:**
- Create: `packages/api/src/lib/files.ts`
- Create: `packages/api/src/lib/files.test.ts`
- Modify: `packages/api/src/routes/list.ts`（全面的に書き換え）

**Interfaces:**
- Consumes: `db`（`packages/api/src/lib/firebase.ts`）
- Produces:
  - `export const HTML_FILES_COLLECTION = "htmlFiles"`
  - `export type FileEntry = { id: string; title: string; description: string; url: string; expiresAt: string; createdAt: string }`
  - `export function resolveBaseUrl(c: Context): string`
  - `export function listFiles(baseUrl: string): Promise<FileEntry[]>`

- [ ] **Step 1: 失敗するテストを書く**

Create `packages/api/src/lib/files.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import type { Context } from "hono";

vi.mock("./firebase.js", () => ({
  db: { collection: vi.fn() },
}));

import { db } from "./firebase.js";
import { resolveBaseUrl, listFiles } from "./files.js";

function makeContext(headers: Record<string, string>): Context {
  return {
    req: { header: (name: string) => headers[name.toLowerCase()] },
  } as unknown as Context;
}

function mockSnapshot(docs: Array<{ id: string; data: Record<string, unknown> }>) {
  const getMock = vi.fn().mockResolvedValue({
    docs: docs.map((d) => ({ id: d.id, data: () => d.data })),
  });
  const orderByMock = vi.fn().mockReturnValue({ get: getMock });
  vi.mocked(db.collection).mockReturnValue({
    orderBy: orderByMock,
  } as unknown as ReturnType<typeof db.collection>);
  return { getMock, orderByMock };
}

describe("resolveBaseUrl", () => {
  it("uses x-forwarded-proto and host when both are present", () => {
    const c = makeContext({ "x-forwarded-proto": "https", host: "api.example.com" });
    expect(resolveBaseUrl(c)).toBe("https://api.example.com");
  });

  it("falls back to http when x-forwarded-proto is absent", () => {
    const c = makeContext({ host: "api.example.com" });
    expect(resolveBaseUrl(c)).toBe("http://api.example.com");
  });

  it("falls back to localhost when host is absent", () => {
    const c = makeContext({});
    expect(resolveBaseUrl(c)).toBe("http://localhost");
  });
});

describe("listFiles", () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it("returns an empty array when there are no documents", async () => {
    mockSnapshot([]);
    await expect(listFiles("http://localhost")).resolves.toEqual([]);
  });

  it("maps Firestore documents to FileEntry with a share URL", async () => {
    mockSnapshot([
      {
        id: "01ABC",
        data: {
          title: "Monthly Report",
          description: "Details",
          expiresAt: { toDate: () => new Date("2026-08-14T00:00:00.000Z") },
          createdAt: { toDate: () => new Date("2026-08-07T00:00:00.000Z") },
        },
      },
    ]);

    const files = await listFiles("https://api.example.com");

    expect(files).toEqual([
      {
        id: "01ABC",
        title: "Monthly Report",
        description: "Details",
        url: "https://api.example.com/s/01ABC",
        expiresAt: "2026-08-14T00:00:00.000Z",
        createdAt: "2026-08-07T00:00:00.000Z",
      },
    ]);
  });

  it("queries the htmlFiles collection ordered by createdAt descending", async () => {
    const { orderByMock } = mockSnapshot([]);
    await listFiles("http://localhost");
    expect(db.collection).toHaveBeenCalledWith("htmlFiles");
    expect(orderByMock).toHaveBeenCalledWith("createdAt", "desc");
  });
});
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

Run: `pnpm --filter @timothy/api run test src/lib/files.test.ts`

Expected: FAIL。`Failed to resolve import "./files.js"` のように、モジュールが存在しないエラーになる。

- [ ] **Step 3: `lib/files.ts` を実装する**

Create `packages/api/src/lib/files.ts`:

```ts
import type { Context } from "hono";
import { db } from "./firebase.js";

export const HTML_FILES_COLLECTION = "htmlFiles";

export type FileEntry = {
  id: string;
  title: string;
  description: string;
  url: string;
  /** ISO 8601 */
  expiresAt: string;
  /** ISO 8601 */
  createdAt: string;
};

export function resolveBaseUrl(c: Context): string {
  const proto = c.req.header("x-forwarded-proto") ?? "http";
  const host = c.req.header("host") ?? "localhost";
  return `${proto}://${host}`;
}

export async function listFiles(baseUrl: string): Promise<FileEntry[]> {
  const snapshot = await db
    .collection(HTML_FILES_COLLECTION)
    .orderBy("createdAt", "desc")
    .get();

  return snapshot.docs.map((doc) => {
    const data = doc.data();
    return {
      id: doc.id,
      title: data.title,
      description: data.description,
      url: `${baseUrl}/s/${doc.id}`,
      expiresAt: data.expiresAt.toDate().toISOString(),
      createdAt: data.createdAt.toDate().toISOString(),
    };
  });
}
```

- [ ] **Step 4: テストを実行して成功することを確認する**

Run: `pnpm --filter @timothy/api run test src/lib/files.test.ts`

Expected: PASS（6 テスト）

- [ ] **Step 5: `routes/list.ts` を書き換える**

Replace the entire contents of `packages/api/src/routes/list.ts`:

```ts
import { Hono } from "hono";
import { listFiles, resolveBaseUrl } from "../lib/files.js";

const app = new Hono();

app.get("/", async (c) => {
  const files = await listFiles(resolveBaseUrl(c));
  return c.json({ files });
});

export default app;
```

- [ ] **Step 6: 全テストとビルドが通ることを確認する**

Run: `pnpm --filter @timothy/api run build && pnpm --filter @timothy/api run test`

Expected: どちらも成功。`GET /files` のレスポンス形状は変わっていないので、既存の結合テストも通る。

- [ ] **Step 7: コミット**

```bash
git add packages/api/src/lib/files.ts packages/api/src/lib/files.test.ts packages/api/src/routes/list.ts
git commit -m "refactor(api): extract file listing into lib/files"
```

---

### Task 3: IP allowlist を Web UI と管理 API に適用する

Web UI を追加すると、ブラウザから叩ける管理画面が公開される。画面だけを守っても、その画面が呼ぶ `/upload` と `/files` が無防備なままでは意味がないので、allowlist の適用範囲を広げる。

`ALLOWED_IPS` が未設定なら全リクエストを通すという既存の挙動は変えない。

**注意:** Hono のワイルドカード `/files/*` は `/files`（末尾スラッシュなし）にはマッチしない。`GET /files` を守るには `app.use("/files", ...)` を別途書く必要がある。

**Files:**
- Modify: `packages/api/src/index.ts`
- Create: `packages/api/src/index.test.ts`

**Interfaces:**
- Consumes: `ipAllowlistMiddleware`（`packages/api/src/lib/ipAllowlist.ts`、既存・変更なし）
- Produces: なし（`index.ts` のミドルウェア構成のみ）

- [ ] **Step 1: 失敗するテストを書く**

Create `packages/api/src/index.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";

vi.mock("./lib/firebase.js", () => ({
  db: { collection: vi.fn() },
  storage: { bucket: vi.fn() },
}));

import app from "./index.js";

const DENIED_IP = "203.0.113.9";

function request(path: string, method = "GET") {
  return app.request(path, {
    method,
    headers: { "x-forwarded-for": DENIED_IP },
  });
}

describe("IP allowlist coverage", () => {
  beforeEach(() => {
    process.env.ALLOWED_IPS = "198.51.100.0/24";
  });

  afterEach(() => {
    delete process.env.ALLOWED_IPS;
  });

  it("blocks the web UI", async () => {
    expect((await request("/")).status).toBe(403);
  });

  it("blocks the upload endpoint", async () => {
    expect((await request("/upload", "POST")).status).toBe(403);
  });

  it("blocks the file list endpoint", async () => {
    expect((await request("/files")).status).toBe(403);
  });

  it("blocks the delete endpoint", async () => {
    expect((await request("/files/01ABC", "DELETE")).status).toBe(403);
  });

  it("blocks the share endpoint", async () => {
    expect((await request("/s/01ABC")).status).toBe(403);
  });

  it("does not block the health endpoint", async () => {
    expect((await request("/health")).status).toBe(200);
  });
});

describe("without ALLOWED_IPS", () => {
  it("does not block the file list endpoint", async () => {
    delete process.env.ALLOWED_IPS;
    const res = await request("/files");
    expect(res.status).not.toBe(403);
  });
});
```

`/` のテストは Task 4 で Web UI を実装するまで 404 を返すが、`ipAllowlistMiddleware` はルート解決の前に走るので 403 が返る。したがってこのテストは Task 4 の前でも成立する。

- [ ] **Step 2: テストを実行して失敗することを確認する**

Run: `pnpm --filter @timothy/api run test src/index.test.ts`

Expected: FAIL。`/upload`、`/files`、`/files/01ABC` の 3 つが 403 ではない（現状 allowlist の対象外のため）。

- [ ] **Step 3: `index.ts` のミドルウェア構成を変更する**

Modify `packages/api/src/index.ts` — `app.use` の並びを次のように書き換える:

```ts
app.use("*", accessLogMiddleware);
app.use("/", ipAllowlistMiddleware);
app.use("/upload", ipAllowlistMiddleware);
app.use("/files", ipAllowlistMiddleware);
app.use("/files/*", ipAllowlistMiddleware);
app.use("/s/*", ipAllowlistMiddleware);
```

`/files` と `/files/*` の両方が必要な点に注意（前者は一覧、後者は削除）。

- [ ] **Step 4: テストを実行して成功することを確認する**

Run: `pnpm --filter @timothy/api run test src/index.test.ts`

Expected: PASS（7 テスト）

- [ ] **Step 5: 全テストが通ることを確認する**

Run: `pnpm --filter @timothy/api run build && pnpm --filter @timothy/api run test`

Expected: どちらも成功

- [ ] **Step 6: コミット**

```bash
git add packages/api/src/index.ts packages/api/src/index.test.ts
git commit -m "feat(api): apply IP allowlist to web UI and management endpoints"
```

---

### Task 4: ファイル一覧ページを SSR する

管理画面の骨格と一覧テーブルを実装する。この時点ではアップロードフォームと JavaScript はまだ入れない（Task 5・6 で追加）。

**Files:**
- Create: `packages/api/src/routes/web.tsx`
- Create: `packages/api/src/routes/web.test.ts`
- Modify: `packages/api/src/index.ts`

**Interfaces:**
- Consumes: `listFiles`, `resolveBaseUrl`, `FileEntry`（Task 2 の `lib/files.ts`）
- Produces:
  - `export function formatJst(iso: string): string` — ISO 8601 を `Asia/Tokyo` の `YYYY-MM-DD HH:mm` に整形
  - `export function isExpired(iso: string, nowMs: number): boolean`
  - `export default app` — `GET /` を持つ Hono アプリ

- [ ] **Step 1: 失敗するテストを書く**

Create `packages/api/src/routes/web.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("../lib/files.js", () => ({
  listFiles: vi.fn(),
  resolveBaseUrl: vi.fn().mockReturnValue("http://localhost"),
}));

import { listFiles } from "../lib/files.js";
import app, { formatJst, isExpired } from "./web.js";

function entry(overrides: Record<string, unknown> = {}) {
  return {
    id: "01ABC",
    title: "Monthly Report",
    description: "Details",
    url: "http://localhost/s/01ABC",
    expiresAt: "2099-01-01T00:00:00.000Z",
    createdAt: "2026-08-07T03:04:00.000Z",
    ...overrides,
  };
}

describe("formatJst", () => {
  it("formats an ISO timestamp as YYYY-MM-DD HH:mm in Asia/Tokyo", () => {
    expect(formatJst("2026-08-07T03:04:00.000Z")).toBe("2026-08-07 12:04");
  });

  it("rolls over to the next day for late UTC timestamps", () => {
    expect(formatJst("2026-08-07T15:30:00.000Z")).toBe("2026-08-08 00:30");
  });
});

describe("isExpired", () => {
  const nowMs = Date.parse("2026-08-07T00:00:00.000Z");

  it("returns false for a future timestamp", () => {
    expect(isExpired("2026-08-08T00:00:00.000Z", nowMs)).toBe(false);
  });

  it("returns true for a past timestamp", () => {
    expect(isExpired("2026-08-06T00:00:00.000Z", nowMs)).toBe(true);
  });
});

describe("GET /", () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it("renders the empty state when there are no files", async () => {
    vi.mocked(listFiles).mockResolvedValue([]);
    const res = await app.request("/");
    expect(res.status).toBe(200);
    expect(res.headers.get("content-type")).toMatch(/text\/html/);
    expect(await res.text()).toContain("まだファイルがありません");
  });

  it("renders a row with the title and share URL", async () => {
    vi.mocked(listFiles).mockResolvedValue([entry()]);
    const html = await (await app.request("/")).text();
    expect(html).toContain("Monthly Report");
    expect(html).toContain("http://localhost/s/01ABC");
    expect(html).not.toContain("まだファイルがありません");
  });

  it("formats timestamps in Asia/Tokyo", async () => {
    vi.mocked(listFiles).mockResolvedValue([entry()]);
    const html = await (await app.request("/")).text();
    expect(html).toContain("2026-08-07 12:04");
  });

  it("marks expired files with a badge", async () => {
    vi.mocked(listFiles).mockResolvedValue([
      entry({ id: "01OLD", expiresAt: "2000-01-01T00:00:00.000Z" }),
    ]);
    const html = await (await app.request("/")).text();
    expect(html).toContain("期限切れ");
  });

  it("does not mark live files as expired", async () => {
    vi.mocked(listFiles).mockResolvedValue([entry()]);
    const html = await (await app.request("/")).text();
    expect(html).not.toContain("期限切れ");
  });

  it("renders a delete button carrying the file id", async () => {
    vi.mocked(listFiles).mockResolvedValue([entry()]);
    const html = await (await app.request("/")).text();
    expect(html).toContain('data-delete-id="01ABC"');
  });

  it("escapes HTML in the title", async () => {
    vi.mocked(listFiles).mockResolvedValue([entry({ title: "<script>alert(1)</script>" })]);
    const html = await (await app.request("/")).text();
    expect(html).not.toContain("<script>alert(1)</script>");
    expect(html).toContain("&lt;script&gt;");
  });

  it("returns 500 with a readable message when listing fails", async () => {
    vi.mocked(listFiles).mockRejectedValue(new Error("Firestore is down"));
    const res = await app.request("/");
    expect(res.status).toBe(500);
    expect(res.headers.get("content-type")).toMatch(/text\/html/);
    expect(await res.text()).toContain("一覧を取得できませんでした");
  });
});
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

Run: `pnpm --filter @timothy/api run test src/routes/web.test.ts`

Expected: FAIL。`Failed to resolve import "./web.js"`

- [ ] **Step 3: `web.tsx` を実装する**

Create `packages/api/src/routes/web.tsx`:

```tsx
import { Hono } from "hono";
import { css, Style } from "hono/css";
import { listFiles, resolveBaseUrl, type FileEntry } from "../lib/files.js";

const JST_FORMATTER = new Intl.DateTimeFormat("sv-SE", {
  timeZone: "Asia/Tokyo",
  year: "numeric",
  month: "2-digit",
  day: "2-digit",
  hour: "2-digit",
  minute: "2-digit",
});

export function formatJst(iso: string): string {
  return JST_FORMATTER.format(new Date(iso));
}

export function isExpired(iso: string, nowMs: number): boolean {
  return Date.parse(iso) < nowMs;
}

const bodyClass = css`
  margin: 0;
  padding: 2rem 1.5rem 4rem;
  font-family: system-ui, -apple-system, "Hiragino Sans", "Noto Sans JP", sans-serif;
  color: #1f2933;
  background: #f7f8fa;
`;

const containerClass = css`
  max-width: 60rem;
  margin: 0 auto;
`;

const tableClass = css`
  width: 100%;
  border-collapse: collapse;
  background: #fff;
  border: 1px solid #e2e6eb;
  border-radius: 8px;
  overflow: hidden;

  th,
  td {
    padding: 0.6rem 0.75rem;
    text-align: left;
    border-bottom: 1px solid #eef1f4;
    font-size: 0.875rem;
    vertical-align: top;
  }

  th {
    background: #f2f4f7;
    font-weight: 600;
  }

  tr:last-child td {
    border-bottom: none;
  }

  tr[data-expired="true"] {
    color: #9aa5b1;
  }
`;

const badgeClass = css`
  display: inline-block;
  padding: 0.1rem 0.4rem;
  margin-left: 0.4rem;
  font-size: 0.75rem;
  border-radius: 4px;
  background: #fde2e2;
  color: #a61b1b;
`;

const emptyClass = css`
  padding: 3rem 1rem;
  text-align: center;
  color: #7b8794;
  background: #fff;
  border: 1px solid #e2e6eb;
  border-radius: 8px;
`;

const errorPageClass = css`
  padding: 3rem 1rem;
  text-align: center;
  color: #a61b1b;
`;

function Layout(props: { children: unknown }) {
  return (
    <html lang="ja">
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Timothy</title>
        <Style />
      </head>
      <body class={bodyClass}>
        <div class={containerClass}>{props.children}</div>
      </body>
    </html>
  );
}

function FileTable(props: { files: FileEntry[]; nowMs: number }) {
  if (props.files.length === 0) {
    return <p class={emptyClass}>まだファイルがありません</p>;
  }

  return (
    <table class={tableClass}>
      <thead>
        <tr>
          <th>タイトル</th>
          <th>説明</th>
          <th>共有 URL</th>
          <th>有効期限</th>
          <th>作成日時</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        {props.files.map((file) => {
          const expired = isExpired(file.expiresAt, props.nowMs);
          return (
            <tr key={file.id} data-expired={String(expired)}>
              <td>{file.title}</td>
              <td>{file.description}</td>
              <td>
                <a href={file.url} target="_blank" rel="noreferrer">
                  {file.url}
                </a>
                <button type="button" data-copy-url={file.url}>
                  コピー
                </button>
              </td>
              <td>
                {formatJst(file.expiresAt)}
                {expired ? <span class={badgeClass}>期限切れ</span> : null}
              </td>
              <td>{formatJst(file.createdAt)}</td>
              <td>
                <button type="button" data-delete-id={file.id}>
                  削除
                </button>
                <span data-row-error></span>
              </td>
            </tr>
          );
        })}
      </tbody>
    </table>
  );
}

const app = new Hono();

app.get("/", async (c) => {
  let files: FileEntry[];
  try {
    files = await listFiles(resolveBaseUrl(c));
  } catch {
    return c.html(
      <Layout>
        <p class={errorPageClass}>一覧を取得できませんでした。時間をおいて再読み込みしてください。</p>
      </Layout>,
      500
    );
  }

  return c.html(
    <Layout>
      <h1>Timothy</h1>
      <FileTable files={files} nowMs={Date.now()} />
    </Layout>
  );
});

export default app;
```

- [ ] **Step 4: テストを実行して成功することを確認する**

Run: `pnpm --filter @timothy/api run test src/routes/web.test.ts`

Expected: PASS（13 テスト）

`formatJst` のテストが `2026-08-07 12:04` ではなく `2026-08-07 12.04` のような結果で落ちる場合は、Node の ICU が縮小版になっている。その場合は `JST_FORMATTER` を使わず、`toLocaleString("sv-SE", { timeZone: "Asia/Tokyo" })` ではなく次の実装に差し替えること:

```ts
export function formatJst(iso: string): string {
  const jst = new Date(Date.parse(iso) + 9 * 60 * 60 * 1000);
  const pad = (n: number) => String(n).padStart(2, "0");
  return `${jst.getUTCFullYear()}-${pad(jst.getUTCMonth() + 1)}-${pad(jst.getUTCDate())} ${pad(jst.getUTCHours())}:${pad(jst.getUTCMinutes())}`;
}
```

- [ ] **Step 5: `index.ts` にルートをマウントする**

Modify `packages/api/src/index.ts`:

import を追加（既存の import 群の末尾）:

```ts
import webRoute from "./routes/web.js";
```

`app.route("/s", serveRoute);` の**後**に 1 行追加:

```ts
app.route("/", webRoute);
```

`/` へのマウントは他のルートより後に置くこと。

- [ ] **Step 6: 全テストとビルドが通ることを確認する**

Run: `pnpm --filter @timothy/api run build && pnpm --filter @timothy/api run test`

Expected: どちらも成功。Task 3 で書いた `index.test.ts` の `/` が 403 を返すテストも引き続き通る。

- [ ] **Step 7: 手で動作を確認する**

Run: `pnpm --filter @timothy/api run dev`

ブラウザで `http://localhost:3000/` を開く。Firestore に接続できない環境なら「一覧を取得できませんでした」が表示されればよい。Firestore エミュレータがあれば一覧が表示されることを確認する。確認したらサーバーを止める。

- [ ] **Step 8: コミット**

```bash
git add packages/api/src/routes/web.tsx packages/api/src/routes/web.test.ts packages/api/src/index.ts
git commit -m "feat(api): render file list page with hono/jsx"
```

---

### Task 5: アップロードフォームのマークアップを追加する

一覧ページの上部にアップロードフォームを置く。この時点ではまだ JavaScript を繋がないので、送信しても何も起きない。マークアップと ID の付与だけを行う。

`POST /upload` の既存バリデーション（`parseUploadRequest`）は `title` / `description` / `ttlDays` の 3 つをすべて必須フィールドとして扱う。`description` は空文字を許容するので、説明が未入力のときは空文字列を送ればよい。`ttlDays` は正の整数のみ受け付ける。

**Files:**
- Modify: `packages/api/src/routes/web.tsx`
- Modify: `packages/api/src/routes/web.test.ts`

**Interfaces:**
- Consumes: Task 4 の `Layout`
- Produces: DOM に次の ID / 属性を持つ要素。Task 6 の JavaScript がこれらを参照する。
  - `#upload-form`（`<form>`）
  - `#drop-zone`（`<div>`、`data-dragging` 属性で状態を表す）
  - `#file-input`（`<input type="file">`）
  - `#title-input`, `#description-input`, `#ttl-input`
  - `#submit-button`
  - `#form-error`（`hidden` 属性付きの `<p>`）

- [ ] **Step 1: 失敗するテストを書く**

Append to the `describe("GET /", ...)` block in `packages/api/src/routes/web.test.ts`:

```ts
  it("renders the upload form with all inputs", async () => {
    vi.mocked(listFiles).mockResolvedValue([]);
    const html = await (await app.request("/")).text();
    expect(html).toContain('id="upload-form"');
    expect(html).toContain('id="drop-zone"');
    expect(html).toContain('id="file-input"');
    expect(html).toContain('id="title-input"');
    expect(html).toContain('id="description-input"');
    expect(html).toContain('id="ttl-input"');
    expect(html).toContain('id="submit-button"');
    expect(html).toContain('id="form-error"');
  });

  it("restricts the file input to HTML files", async () => {
    vi.mocked(listFiles).mockResolvedValue([]);
    const html = await (await app.request("/")).text();
    expect(html).toContain('accept=".html,.htm"');
  });

  it("defaults the TTL to 7 days and only accepts positive integers", async () => {
    vi.mocked(listFiles).mockResolvedValue([]);
    const html = await (await app.request("/")).text();
    expect(html).toContain('value="7"');
    expect(html).toContain('min="1"');
    expect(html).toContain('step="1"');
  });

  it("hides the error area by default", async () => {
    vi.mocked(listFiles).mockResolvedValue([]);
    const html = await (await app.request("/")).text();
    expect(html).toMatch(/id="form-error"[^>]*hidden/);
  });

  it("does not render the upload form on the error page", async () => {
    vi.mocked(listFiles).mockRejectedValue(new Error("Firestore is down"));
    const html = await (await app.request("/")).text();
    expect(html).not.toContain('id="upload-form"');
  });
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

Run: `pnpm --filter @timothy/api run test src/routes/web.test.ts`

Expected: FAIL（新しく追加した 4 件が失敗。最後の 1 件はエラーページにフォームがないので最初から通る）

- [ ] **Step 3: `UploadForm` を実装する**

Add to `packages/api/src/routes/web.tsx`, after the `badgeClass` definition:

```tsx
const formClass = css`
  margin-bottom: 2rem;
  padding: 1.25rem;
  background: #fff;
  border: 1px solid #e2e6eb;
  border-radius: 8px;

  label {
    display: block;
    margin-bottom: 0.75rem;
    font-size: 0.875rem;
    font-weight: 600;
  }

  input[type="text"],
  input[type="number"] {
    display: block;
    width: 100%;
    max-width: 24rem;
    margin-top: 0.25rem;
    padding: 0.4rem 0.5rem;
    font: inherit;
    font-weight: 400;
    border: 1px solid #cbd2d9;
    border-radius: 4px;
  }
`;

const dropZoneClass = css`
  margin-bottom: 1rem;
  padding: 1.5rem;
  text-align: center;
  color: #7b8794;
  border: 2px dashed #cbd2d9;
  border-radius: 8px;

  &[data-dragging="true"] {
    border-color: #2f80ed;
    background: #eef5fe;
    color: #2f80ed;
  }
`;

const errorBoxClass = css`
  margin: 0 0 1rem;
  padding: 0.6rem 0.75rem;
  font-size: 0.875rem;
  color: #a61b1b;
  background: #fde2e2;
  border-radius: 4px;

  &[hidden] {
    display: none;
  }
`;

function UploadForm() {
  return (
    <form id="upload-form" class={formClass}>
      <p id="form-error" class={errorBoxClass} hidden></p>
      <div id="drop-zone" class={dropZoneClass} data-dragging="false">
        ここに HTML ファイルをドラッグ&amp;ドロップ
      </div>
      <label>
        ファイル
        <input id="file-input" type="file" name="file" accept=".html,.htm" required />
      </label>
      <label>
        タイトル
        <input id="title-input" type="text" name="title" required />
      </label>
      <label>
        説明（任意）
        <input id="description-input" type="text" name="description" />
      </label>
      <label>
        有効期間（日）
        <input id="ttl-input" type="number" name="ttlDays" value="7" min="1" step="1" required />
      </label>
      <button id="submit-button" type="submit">
        アップロード
      </button>
    </form>
  );
}
```

Then render it in the success branch of `app.get("/")`, between the `<h1>` and `<FileTable />`:

```tsx
      <h1>Timothy</h1>
      <UploadForm />
      <FileTable files={files} nowMs={Date.now()} />
```

- [ ] **Step 4: テストを実行して成功することを確認する**

Run: `pnpm --filter @timothy/api run test src/routes/web.test.ts`

Expected: PASS（18 テスト）

- [ ] **Step 5: ビルドと全テストを確認する**

Run: `pnpm --filter @timothy/api run build && pnpm --filter @timothy/api run test`

Expected: どちらも成功

- [ ] **Step 6: コミット**

```bash
git add packages/api/src/routes/web.tsx packages/api/src/routes/web.test.ts
git commit -m "feat(api): add upload form markup to web UI"
```

---

### Task 6: クライアント JavaScript を繋ぐ

アップロード（署名付き URL 経由）、ドラッグ&ドロップ、削除、URL コピーを動作させる。

スクリプトは文字列定数として別ファイルに置き、`web.tsx` からは `<script>` に流し込むだけにする。`web.tsx` をマークアップの責務に集中させるため。

**Files:**
- Create: `packages/api/src/routes/webScript.ts`
- Modify: `packages/api/src/routes/web.tsx`
- Modify: `packages/api/src/routes/web.test.ts`

**Interfaces:**
- Consumes: Task 5 が定義した DOM の ID / 属性（`#upload-form`, `#drop-zone`, `#file-input`, `#title-input`, `#description-input`, `#ttl-input`, `#submit-button`, `#form-error`, `[data-delete-id]`, `[data-copy-url]`, `[data-row-error]`）
- Produces: `export const CLIENT_SCRIPT: string`

- [ ] **Step 1: 失敗するテストを書く**

Append to the `describe("GET /", ...)` block in `packages/api/src/routes/web.test.ts`:

```ts
  it("embeds the client script on the list page", async () => {
    vi.mocked(listFiles).mockResolvedValue([]);
    const html = await (await app.request("/")).text();
    expect(html).toContain("addEventListener");
    expect(html).toContain('"/upload"');
  });

  it("does not embed the client script on the error page", async () => {
    vi.mocked(listFiles).mockRejectedValue(new Error("Firestore is down"));
    const html = await (await app.request("/")).text();
    expect(html).not.toContain("addEventListener");
  });
```

Also add a new top-level describe block at the end of the file:

```ts
describe("CLIENT_SCRIPT", () => {
  it("does not contain a closing script tag that would break embedding", async () => {
    const { CLIENT_SCRIPT } = await import("./webScript.js");
    expect(CLIENT_SCRIPT).not.toContain("</script>");
  });
});
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

Run: `pnpm --filter @timothy/api run test src/routes/web.test.ts`

Expected: FAIL。`Failed to resolve import "./webScript.js"` と、スクリプト埋め込みのテストが失敗する。

- [ ] **Step 3: `webScript.ts` を実装する**

Create `packages/api/src/routes/webScript.ts`:

```ts
/**
 * 管理画面に埋め込むクライアントスクリプト。
 * バンドラを持ち込まないため、素の JavaScript を文字列として保持する。
 */
export const CLIENT_SCRIPT = `
(function () {
  var form = document.getElementById("upload-form");
  var fileInput = document.getElementById("file-input");
  var titleInput = document.getElementById("title-input");
  var descriptionInput = document.getElementById("description-input");
  var ttlInput = document.getElementById("ttl-input");
  var submitButton = document.getElementById("submit-button");
  var dropZone = document.getElementById("drop-zone");
  var errorBox = document.getElementById("form-error");

  function showError(message) {
    errorBox.textContent = message;
    errorBox.hidden = false;
  }

  function clearError() {
    errorBox.textContent = "";
    errorBox.hidden = true;
  }

  function isHtmlFile(file) {
    return /\\.html?$/i.test(file.name);
  }

  function baseName(name) {
    return name.replace(/\\.html?$/i, "");
  }

  function fillTitleIfEmpty(file) {
    if (!titleInput.value) titleInput.value = baseName(file.name);
  }

  ["dragenter", "dragover"].forEach(function (type) {
    dropZone.addEventListener(type, function (e) {
      e.preventDefault();
      dropZone.dataset.dragging = "true";
    });
  });

  ["dragleave", "drop"].forEach(function (type) {
    dropZone.addEventListener(type, function (e) {
      e.preventDefault();
      dropZone.dataset.dragging = "false";
    });
  });

  dropZone.addEventListener("drop", function (e) {
    var file = e.dataTransfer && e.dataTransfer.files[0];
    if (!file) return;
    if (!isHtmlFile(file)) {
      showError("HTML ファイル (.html / .htm) を選択してください");
      return;
    }
    clearError();
    var transfer = new DataTransfer();
    transfer.items.add(file);
    fileInput.files = transfer.files;
    fillTitleIfEmpty(file);
  });

  fileInput.addEventListener("change", function () {
    var file = fileInput.files[0];
    if (file) fillTitleIfEmpty(file);
  });

  form.addEventListener("submit", async function (e) {
    e.preventDefault();
    clearError();

    var file = fileInput.files[0];
    if (!file) {
      showError("ファイルを選択してください");
      return;
    }
    if (!isHtmlFile(file)) {
      showError("HTML ファイル (.html / .htm) を選択してください");
      return;
    }

    submitButton.disabled = true;
    submitButton.textContent = "アップロード中...";

    try {
      var html = await file.text();

      var res = await fetch("/upload", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          title: titleInput.value,
          description: descriptionInput.value,
          ttlDays: Number(ttlInput.value),
        }),
      });

      if (!res.ok) {
        var body = await res.json().catch(function () { return {}; });
        showError(body.error || "アップロードの準備に失敗しました (HTTP " + res.status + ")");
        return;
      }

      var issued = await res.json();
      var put = await fetch(issued.uploadUrl, {
        method: "PUT",
        headers: issued.uploadHeaders,
        body: html,
      });

      if (!put.ok) {
        showError(
          "アップロードに失敗しました (HTTP " + put.status + ")。" +
          "ファイル情報だけが登録されている場合があります。不要なら一覧から削除してください。"
        );
        return;
      }

      location.reload();
    } catch (err) {
      showError(
        "アップロードに失敗しました: " + err.message +
        "。ストレージバケットの CORS 設定を確認してください。"
      );
    } finally {
      submitButton.disabled = false;
      submitButton.textContent = "アップロード";
    }
  });

  document.querySelectorAll("[data-delete-id]").forEach(function (button) {
    button.addEventListener("click", async function () {
      if (!confirm("このファイルを削除しますか？")) return;

      var row = button.closest("tr");
      var rowError = row.querySelector("[data-row-error]");
      rowError.textContent = "";
      button.disabled = true;

      try {
        var res = await fetch("/files/" + button.dataset.deleteId, { method: "DELETE" });
        if (!res.ok) {
          rowError.textContent = "削除に失敗しました (HTTP " + res.status + ")";
          button.disabled = false;
          return;
        }
        location.reload();
      } catch (err) {
        rowError.textContent = "削除に失敗しました: " + err.message;
        button.disabled = false;
      }
    });
  });

  document.querySelectorAll("[data-copy-url]").forEach(function (button) {
    button.addEventListener("click", async function () {
      try {
        await navigator.clipboard.writeText(button.dataset.copyUrl);
        var original = button.textContent;
        button.textContent = "コピーしました";
        setTimeout(function () { button.textContent = original; }, 1500);
      } catch (err) {
        /* クリップボードが使えない環境では何もしない */
      }
    });
  });
})();
`;
```

`\\.html?$` のバックスラッシュ 2 個は、テンプレートリテラル内で正規表現の `\.` を表すために必要。

- [ ] **Step 4: `web.tsx` にスクリプトを埋め込む**

Add the import at the top of `packages/api/src/routes/web.tsx`:

```tsx
import { CLIENT_SCRIPT } from "./webScript.js";
```

Change `Layout` to accept an optional script flag:

```tsx
function Layout(props: { children: unknown; withScript?: boolean }) {
  return (
    <html lang="ja">
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Timothy</title>
        <Style />
      </head>
      <body class={bodyClass}>
        <div class={containerClass}>{props.children}</div>
        {props.withScript ? (
          <script dangerouslySetInnerHTML={{ __html: CLIENT_SCRIPT }}></script>
        ) : null}
      </body>
    </html>
  );
}
```

Then set `withScript` on the success branch only:

```tsx
  return c.html(
    <Layout withScript>
      <h1>Timothy</h1>
      <UploadForm />
      <FileTable files={files} nowMs={Date.now()} />
    </Layout>
  );
```

The error branch keeps `<Layout>` without `withScript`, since none of the elements the script looks for exist there.

- [ ] **Step 5: テストを実行して成功することを確認する**

Run: `pnpm --filter @timothy/api run test src/routes/web.test.ts`

Expected: PASS（21 テスト）

- [ ] **Step 6: ビルドと全テストを確認する**

Run: `pnpm --filter @timothy/api run build && pnpm --filter @timothy/api run test && pnpm lint`

Expected: すべて成功

- [ ] **Step 7: 手で動作を確認する**

Firestore エミュレータを使って一連の動作を確認する。

```bash
pnpm --filter @timothy/api run dev
```

ブラウザで `http://localhost:3000/` を開き、次を確認する:

1. ドロップ領域に `.html` ファイルをドラッグすると枠線が青くなる
2. ドロップするとファイルがセットされ、タイトルが空ならファイル名で埋まる
3. `.txt` をドロップするとエラーメッセージが出る
4. アップロードすると一覧に行が増える（GCS のエミュレータがない環境では PUT が失敗し、CORS を案内するエラーが出るのが正しい挙動）
5. コピーボタンで URL がクリップボードに入る
6. 削除ボタンで確認ダイアログが出て、OK すると行が消える

確認したらサーバーを止める。

- [ ] **Step 8: コミット**

```bash
git add packages/api/src/routes/webScript.ts packages/api/src/routes/web.tsx packages/api/src/routes/web.test.ts
git commit -m "feat(api): wire up upload, drag-and-drop, delete and copy in web UI"
```

---

### Task 7: ドキュメントを更新する

Web UI の使い方、GCS バケットの CORS 設定、`ALLOWED_IPS` の適用範囲の変更を README に反映する。CORS 設定が抜けていると Web からのアップロードだけが失敗するので、セルフホスティング手順の必須項目として書く。

**Files:**
- Modify: `README.md`
- Modify: `README.ja.md`

**Interfaces:**
- Consumes: なし
- Produces: なし

- [ ] **Step 1: 既存の README の構成を読む**

Run: `cat README.md && cat README.ja.md`

「Features」「Usage」「Self-Hosting」の各節の見出しレベルと書き方を把握する。以降の追記はその書き方に揃える。

- [ ] **Step 2: `README.md` に Web UI の節を追加する**

`Features` のリストに 1 項目追加:

```markdown
- Browse and upload files from a web UI served by the API
```

`Usage` の節（`List` の後あたり）に次を追加:

```markdown
### Web UI

Open your API endpoint in a browser to get a management page:

```
https://your-api.example.com/
```

From there you can browse uploaded files, upload a new HTML file by picking it
or dragging it onto the page, copy share URLs, and delete files.

The web UI uploads through the same signed-URL flow as the CLI, so it requires
CORS to be configured on your storage bucket. See [Self-Hosting](#self-hosting).
```

- [ ] **Step 3: `README.md` のセルフホスティング手順に CORS 設定を追加する**

`Self-Hosting` の節に次を追加:

```markdown
### Storage bucket CORS

The web UI uploads directly from the browser to Cloud Storage using a signed
URL. Without CORS configured on the bucket, those uploads fail (the CLI is
unaffected).

Create `cors.json`, replacing the origin with your API endpoint:

```json
[
  {
    "origin": ["https://your-api.example.com"],
    "method": ["PUT"],
    "responseHeader": ["Content-Type"],
    "maxAgeSeconds": 3600
  }
]
```

Apply it:

```bash
gcloud storage buckets update gs://YOUR_BUCKET --cors-file=cors.json
```
```

- [ ] **Step 4: `README.md` の `ALLOWED_IPS` の説明を更新する**

`ALLOWED_IPS` に触れている箇所に次の注記を加える:

```markdown
`ALLOWED_IPS` applies to the web UI (`/`), the management endpoints
(`/upload`, `/files`) and the share endpoint (`/s/*`). When it is unset, all
requests are allowed. Note that setting it also restricts the CLI to the
listed addresses.
```

`ALLOWED_IPS` の説明が README に存在しない場合は、`Self-Hosting` の環境変数の節に新規に追加すること。

- [ ] **Step 5: `README.ja.md` に同じ内容を日本語で追加する**

`README.md` に加えた 3 箇所と同じ内容を、`README.ja.md` の対応する節に日本語で追加する。`cors.json` と `gcloud` コマンドはそのまま流用し、説明文だけ日本語にする。

機能一覧に加える項目:

```markdown
- API が配信する Web UI からファイルの一覧表示とアップロードができる
```

- [ ] **Step 6: 相互リンクとコードブロックが壊れていないか確認する**

Run: `git diff README.md README.ja.md`

ネストしたコードブロック（CORS の節は ```markdown の中に ```json が入る形になりやすい）が崩れていないことを目視で確認する。実際の README には ```json と ```bash がトップレベルで並ぶ形で書くこと。

- [ ] **Step 7: コミット**

```bash
git add README.md README.ja.md
git commit -m "docs: document web UI, bucket CORS and ALLOWED_IPS scope"
```

---

## 完了条件

すべてのタスクを終えた時点で次が成り立つこと。

- [ ] `pnpm --filter @timothy/api run build` が成功する
- [ ] `pnpm --filter @timothy/api run test` が成功し、カバレッジ閾値を下回らない
- [ ] `pnpm lint` が成功する
- [ ] `http://localhost:3000/` で一覧・アップロード・削除ができる
- [ ] `GET /files` の JSON レスポンス形状が変わっていない（CLI の互換性）
- [ ] README に CORS 設定と `ALLOWED_IPS` の適用範囲が書かれている
